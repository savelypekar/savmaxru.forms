import {Propertychangemanager} from '../../src/propertychangemanager';

describe('Propertychangemanager', () => {
	it('Should be a function', () => {
		assert(typeof Propertychangemanager === 'function');
	});
});