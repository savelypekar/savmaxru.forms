import {Type} from 'main.core';

//
import {Objectgui} from 'savmaxru.forms.gui.components.objectgui';

export class Dropdownlist extends Objectgui
{
	constructor(options = {})
	{
		/**
		 * @memberOf Savmaxru.Forms.GUI.Components
		 */
alert(8);
	if(Type.isString('')){

	};
		super();
		//alert(111);
		//super(options);

		//super(options);
		//super();
		//super({name: 'Dropdownlist'});

		//this.name = options.name;

		//let parent = document.getElementById(options.parentID);
		//let template = document.getElementById(options.templateID);

		//let object = document.createElement('div');
		//object.append(template.content.cloneNode(true));

	//	parent.append(object);
	}
}